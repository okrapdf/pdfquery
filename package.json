{
  "name": "pdfquery",
  "version": "0.1.2",
  "description": "jQuery for PDFs - query document entities with CSS-like selectors",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "fixtures",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:plugins": "pnpm -C pdfquery-plugins build",
    "test:plugins": "pnpm -C pdfquery-plugins test",
    "build:all": "pnpm -C pdfquery-plugins build && pnpm run build",
    "test:all": "pnpm -C pdfquery-plugins test && pnpm run test",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "pdf",
    "query",
    "jquery",
    "document",
    "ocr",
    "extraction",
    "dom",
    "selector"
  ],
  "author": "pdfquery contributors",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pdfquery/pdfquery"
  },
  "homepage": "https://github.com/pdfquery/pdfquery#readme",
  "devDependencies": {
    "@types/jsdom": "^27.0.0",
    "jsdom": "^27.4.0",
    "sharp": "^0.33.5",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
